[run]
branch = True
omit = lib/*
       piface_webhooks/tests/*
       setup.py
       piface_webhooks/settings.py

[report]
exclude_lines =
    # this cant ever be run by py.test, but it just calls one function,
    # so ignore it
    if __name__ == .__main__.:
    if sys.version_info.+
    raise NotImplementedError
    except ImportError:
